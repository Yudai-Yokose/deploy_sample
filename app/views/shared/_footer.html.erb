<footer class="fixed bottom-0 left-0 w-full bg-transparent text-neutral-content font-bold shadow-none">
  <div class="flex justify-around items-center py-3">
    <a href="/all" class="flex flex-col items-center">
      <i class="fas fa-th-list text-2xl"></i>
      <span class="text-sm mt-2">All</span>
    </a>
    <a href="/yours" class="flex flex-col items-center">
      <i class="fas fa-user-friends text-2xl"></i>
      <span class="text-sm mt-2">Yours</span>
    </a>
    <a href="/record" class="flex flex-col items-center">
      <i class="fas fa-plus-circle text-4xl text-primary"></i>
      <span class="text-sm mt-2">Record</span>
    </a>

    <% if user_signed_in? %>
      <a href="/diary" class="flex flex-col items-center">
        <i class="fas fa-book text-2xl"></i>
        <span class="text-sm mt-2">Diary</span>
      </a>
      <a href="/profile" class="flex flex-col items-center">
        <i class="fas fa-user-circle text-2xl"></i>
        <span class="text-sm mt-2">Profile</span>
      </a>
    <% else %>
      <button class="flex flex-col items-center" onclick="signUpModal.showModal()">
        <i class="fas fa-user-plus text-2xl"></i>
        <span class="text-sm mt-2">Sign up</span>
      </button>
      <button class="flex flex-col items-center" onclick="loginModal.showModal()">
        <i class="fas fa-sign-in-alt text-2xl"></i>
        <span class="text-sm mt-2">Login</span>
      </button>
    <% end %>
  </div>
</footer>

<!-- Sign Up Modal -->
<dialog id="signUpModal" class="modal">
  <div class="modal-box">
    <%= render 'devise/registrations/new'%>
  </div>
  <form method="dialog" class="modal-backdrop">
    <button>Close</button>
  </form>
</dialog>

<!-- Login Modal -->
<dialog id="loginModal" class="modal">
  <div class="modal-box">
    <%= render 'devise/sessions/new'%>
  </div>
  <form method="dialog" class="modal-backdrop">
    <button>Close</button>
  </form>
</dialog>